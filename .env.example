API_BASE_URL=http://localhost:3001
# Same backend, but exposed to the browser — the admin panel (majourney)
# calls it directly from the client per the backend's Bearer-JWT/no-cookie
# design (frontend and backend are on unrelated domains).
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
